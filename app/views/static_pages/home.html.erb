<h1>Simple Calorie Tracker</h1>
<% if signed_in? %>

<div>
    <h3>Calorie counter</h3>
    <%= current_user.email %>
    <%= current_user.trackers %>
    <%= form_for(@tracker) do |f| %>
      <%= f.text_field :calories, placeholder: "Calories" %>
      <%= f.submit "Submit" %>
    <% end %>
    <%= form_for(@tracker) do |f| %>
      <%= f.text_field :consumed_on, placeholder: Date.today %>
      <%= f.submit "Submit" %>
    <% end %>
</div>
<% else %>
<p>A super simple way to track what comes in.</p>
<% end %>
